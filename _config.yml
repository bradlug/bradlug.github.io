name: BradLUG
title: BradLUG
markdown: kramdown
permalink: /blog/:year/:month/:day/:title
exclude:
  - Gemfile
  - Gemfile.lock
theme: jekyll-theme-hacker
paginate: 10
paginate_path: "/:num/"
plugins:
    - jekyll-feed
    - jekyll-paginate
    - jekyll-redirect-from
host: 0.0.0.0
